package com.example.PragyaShipping.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.example.PragyaShipping.entity.QuoteSettings;

public interface QuoteSettingsRepository extends JpaRepository<QuoteSettings, Long> {
}
